package com.dereekb.gae.model.crud.services;

/**
 * A pre-configued {@link CrudService} that takes in more simple arguments, and
 * auto-unwraps results if available.
 *
 * @author dereekb
 *
 */
public interface SimpleCrudService {

	// TODO: Service that just takes in single arguments and returns arrays,
	// like
	// previously.

}
